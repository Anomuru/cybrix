import {memo} from 'react';

import {Button} from "shared/ui/button/button";
import {Input} from "shared/ui/input";

import cls from "./projectsHeader.module.sass";

export const ProjectsHeader = memo(() => {
    return (
        <div></div>
    )
})
